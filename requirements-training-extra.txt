# OPTIONAL extras for training. Only install what you actually need — these
# are heavier, platform-specific, or require a GPU/build toolchain, so they're
# kept separate from requirements-training.txt (the core set needed to run
# core/training/* at all).
#
# Install with: pip install -r requirements-training-extra.txt

# RLHF / DPO training (TrainingStrategy.RLHF / TrainingStrategy.DPO)
trl>=0.7.0

# Multi-GPU / distributed training
deepspeed>=0.12.0
fairscale>=0.4.0

# Experiment tracking
wandb>=0.16.0
mlflow>=2.11.0

# Larger-scale data pipelines
dask>=2023.11.0

# ONNX export
optimum>=1.14.0
onnxruntime>=1.17.0

# GGUF export for llama.cpp (requires a C++ build toolchain)
llama-cpp-python>=0.2.0

# Development / testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.12.0
isort>=5.13.0
